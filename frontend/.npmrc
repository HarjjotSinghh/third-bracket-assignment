# Use npm for better compatibility with Vercel
package-manager=npm

# Ensure proper resolution of peer dependencies
legacy-peer-deps=true

# Allow postinstall scripts but handle them carefully
ignore-scripts=false

# Set registry for better performance
registry=https://registry.npmjs.org/

# Handle optional dependencies properly
optional=true
