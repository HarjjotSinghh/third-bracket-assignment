{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "installCommand": "npm install --legacy-peer-deps --ignore-scripts=false",
  "devCommand": "npm run dev",
  "env": {
    "NODE_ENV": "production"
  }
}
