# Better Auth Configuration
BETTER_AUTH_SECRET=better-auth-secret-key-change-in-production
BETTER_AUTH_URL=http://localhost:3000
FRONTEND_URL=http://localhost:5173

# Database
# Since we're using memory adapter for now, no database config needed
# For production, you'd configure PostgreSQL or another supported database

# Server Configuration
PORT=3000
NODE_ENV=development

# Existing Environment Variables (if any)
MONGODB_URI=mongodb://localhost:27017/task-management
JWT_SECRET=your-jwt-secret-here